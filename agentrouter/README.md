# AgentRouter 签到

基于原始 AgentRouter 签到逻辑，仅增加多账号之间的随机签到抖动。当前版本：r8.3。

## Loon

导入 `AgentRouter_Checkin.plugin`。默认每天 09:00 执行。

- 单账号：填写账号、密码
- 多账号：在“多账号（JSON）”填写原脚本要求的 JSON 数组
- 第一个账号立即执行
- 从第二个账号开始，每个账号随机等待 0～300 秒
- 登录、余额查询、签到记录确认等原逻辑未改变

随机抖动只影响账号开始处理的时间，不修改 AgentRouter 的签到判定逻辑。
